services:
  demo:
    image: php:8.3-cli-bookworm
    working_dir: /workspace
    volumes:
      - ../..:/workspace/packages/laravel-queue-worker
      - ../../laravel-queue-consumer:/workspace/packages/laravel-queue-consumer
      - demo-apps:/workspace/apps
    command: sleep infinity

volumes:
  demo-apps:
